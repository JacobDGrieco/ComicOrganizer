{
	"scripts": {
		"sort": "python -m sorting.project sort --project",
		"sort:dry": "python -m sorting.project sort --dry-run --project",
		"missing": "python -m sorting.project missing --project",
		"list": "python -m sorting.project list --project",
		"reindex": "python -m sorting.project reindex --apply --project",
		"reindex:dry": "python -m sorting.project reindex --project",
		"db:validate": "python -m sorting.project db-validate --project",
		"download": "python -m sorting.project download --project",
		"download:headless": "python -m sorting.project download --headless --project",
		"flatten": "python -m sorting.project flatten --apply --project",
		"flatten:dry": "python -m sorting.project flatten --project",
		"characters": "python -m sorting.project characters --project",
		"convert": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/comic_converter.ps1",
		"check": "python -m compileall -q sorting scripts"
	},
	"dependencies": {
		"playwright": "^1.62.1"
	}
}
